# 로또
#로또 구입 금액을 입력하면 구입 금액에 해당하는 로또를 발급해야 한다.
# 로또 1장의 가격은 1000원이다. -> 매직넘버
#로또 자동 생성은 Collections.shuffle() 메소드 활용한다.
# Collections.sort() 메소드를 활용해 정렬 가능하다.
# ArrayList의 contains() 메소드를 활용하면 어떤 값이 존재하는지 유무를 판단할 수 있다.
# Lotto 클래스 필요한 사항 : 당첨금액(enum?), 맞춘갯수,
# 수익률계산 클래스 필요한 사항 : List<Lotto>, 손익률


# ------------------- 완료 ----------------------
# 그리고 null을 할당하는것 보다 빈 리스트를 할당해주시면 어떨까요 :) ?
# -> null 변경. 자동차 경주 때도 자주 들었던 사항인데 습관이 무섭네요ㅠㅠ  꼼꼼히 코딩하겠습니다.

#사용하지 않는 import는 지워주세요!
#-> import 제거

#입력값을 음수 또는 1000원 미만의 값을 입력하였는데 게임이 진행이 되네요 :)
#-> 게임 진행되게 변경 

#당첨 번호 입력시 공백도 분리해주시면 좋을 것 같아요 :)
#-> 공백도 분리되게 변경

#해당 인스턴스 필드만 접근 제한자가 없는 이유가 있을까요 :) ?
#-> 접근 제한자 지정

#List 인터페이스가 아닌 ArrayList 구현체로 선언하신 이유가 있을까요 :) ?
#-> 이게 에러가 났었는데, 수정후에 에러가 안납니다..해당 부분 수정했습니다.

#메서드 명은 동사구로 시작해주세요!
#-> 메서드 명 동사구로 시작하게 변경

#해당 메서드명을 통해 랜덤으로 섞어주는걸 유추할 수 있는데요.
# 내부에서는 랜덤으로 섞을뿐만 아니라 정렬까지 하고 있습니다.
# 하나의 메서드는 하나의 동작만 해주도록 해주세요!
# -> 메서드 분리

#돈과 같은 1000단위로 구분은 다음과 같이 underbar(_)를 활용하면 코드의 가독성이 좋아질 수 있어요!
# -> Integer에서 underbar를 지원하는건가요..? 

# 접근제한자를 생략해주신 이유가 있을까요 :) ?
# -> 접근 제한자 수정

# 해당 코멘트와 같은 내용입니다!
# -> List로 변경


#해당 객체는 구분 문자를 분리하는 일회성 목적이 강한 것 같아요.
# 그렇기에 상태값을 가지지않아도 된다고 생각하며, 해당 메서드는 정적으로 변경해도 좋을 것 같은데
# 어떻게 생각하시나요 :) ?
# -> 정적으로 변경 


#if문 과 괄호 사이에 공백 넣어주시는게 컨벤션입니다.
# 다음 링크를 참고하시면 좋을 것 같아요 :)
# -> 공백 추가

#하나의 문장으로 줄일 수 있을 것 같아요 :)
# -> 변경

#현재 도메인 영역에서 출력을 하고있네요!
# 핵심 비지니스 로직을 가지는 객체에서 어떻게 그려지는지에 대한 관심은 분리되어야할 것 같습니다.
# 해당 작업은 View 에서 이루어져야 하지 않을까요?
# 비지니스 로직과 출력 로직을 책임지고 있기 때문에 단일 책임의 원칙에도 위배가 되는 것 같아요!
# View 같은 세부 정보를 참조하여 복잡성을 더 추가한다면 복잡도가 더 증가하고 테스트 하기가 어렵지 않을까 해요!
# 그렇기에 출력에 관련된 로직은 출력 객체에게 위임하는게 좋지 않을까요 :) ?
# -> 출력단에서 나오게 변경

# Lotto 객체가 winningCount를 가질 필요가 있을지 고민해보아요 :)
# -> ENUM 사용법을 잘 몰라서 작업에 오류가 좀 있었던것 같습니다 해당 부분 수정했습니다.
#valueOf 메서드와 중복되는것 같아요.
# 해당 메서드는 제거해도 되지 않을까요 :) ?
#-> 삭제했습니다.

# i 라는 네이밍보다 의미를 쉽게 파악할 수 있는 네이밍을 해주시면 좋을 것 같아요 :)
# -> 네이밍 변경

#-------------------------------완료 -------------------------------------

#로또 번호는 1 ~ 45 까지 고정적인 범위를 갖고 있는데요.
# 로또를 생성할때마다 1 ~ 45개의 로또 번호를 가진 리스트를 생성하는 것보다
# 캐싱하여 재사용하도록 변경하면 어떨까요 :) ?
# -> static 리스트로 변경



#현재 각 step 패키지마다 코드가 중복되고 있네요!
# 각 패키지마다 미션을 만들지말고 하나의 패키지에서 미션을 수행해주시면 좋을 것 같아요!
# -> 미션 3때 도메인과 컨트롤러를 제외한 값을든 분리해서 작성하도록 하겠습니다.



#int 값 하나 자체는 그냥 아무 의미 없는 스칼라 값일 뿐이라고 생각하는데요!
# 로또 번호를 단순히 int로 관리하는것보다 VO 객체로 관리하면 좋을 것 같아요 :)
# VO 객체로 관리하면 어떤 장점이 있을까요?
# -> 제가 스칼라 값에 대한 부분을 찾아보니, 
# 확장에 용이하다고 하더라구요
# 말씀하시는게 private static final int LOTTO_NUMBER = 6; 이 부분에 대한 객체 이용을 말씀하시는건지요! ㅠㅠ









