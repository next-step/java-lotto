package step1stringcalculator;

public enum MathOperator {
   /* private final String operator;
    MathOperator(String operator) {
        this.operator = operator;
    }

    SUM("+") {
        public int sum (int defaultNumber, int nextNumber) {
            return defaultNumber + nextNumber;
        }
    };
*/


}
