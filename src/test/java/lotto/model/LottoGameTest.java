package lotto.model;

import org.junit.jupiter.api.DisplayName;
import org.junit.jupiter.api.Test;

import java.util.Arrays;
import java.util.List;

class LottoGameTest {
    @Test
    @DisplayName("당첨 번호 6개를 입력하면 로또 티켓에 숫자가 몇개 일치하는지 반환한다.")
    void test1() {
//        var lottoTicket1 = new LottoTicket(List.of(1,2 3,4,5,6));
//        var lottoTicket2 = new LottoTicket(List.of(1,2 3,4,5,7));
//        var lottoTicketBundle1 = new LottoTicketBundle(Arrays.asList(lottoTicket1));
//        var lottoTicketBundle2 = new LottoTicketBundle(Arrays.asList(lottoTicket2));
//        LottoGame lottoGame = new LottoGame(lottoTicketBundle1, lottoTicketBundle2);
//
//        var winningNumbers =  new LottoTicket(List.of(1,2 3,4,5,6));
//        var bounsNumber = LottoNumber.of(7);
//        WinningLotto winningLotto = new WinningLotto(winningNumbers, bounsNumber);
//        winningLotto.match(lottoTicketBundle1);
    }
}